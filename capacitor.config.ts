import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gamerstechnology.app',
  appName: 'Gamers Technology',
  webDir: 'dist/gamers-technology/browser'
};

export default config;
